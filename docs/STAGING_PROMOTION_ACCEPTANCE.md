# 격리 데이터·후보 DB·최종 반영 차단 인수시험

## 결과

- 실행일: 2026-09-07
- 실행번호: `20260907T095021891136`
- 종합 상태: **PASS**
- 승인 입력 테이블: 5개
- 후보 DB 생성: **READY_CANDIDATE**
- 운영 반영 시도: **BLOCKED (의도한 결과)**
- 기존 운영 DB 변경: 없음

## 확인한 전체 흐름

1. 합성 기업 원본을 표준 형식으로 변환했습니다.
2. 부품 LOT, 공정검사, 차량 장착이력, 보증수리, 비용 기준을 각각 승인된 격리 파일로 저장했습니다.
3. 다섯 파일의 참조관계와 값 형식을 다시 검사했습니다.
4. 실제 규칙 기반 위험분석으로 후보 DB를 생성했습니다.
5. 현재 DB와 후보 DB의 LOT·위험등급·영향 VIN·예상 순편익을 비교했습니다.
6. 합성 표본이 운영 의사결정 최소 기준에 미달하므로 최종 반영을 차단했습니다.

## 재현 명령

```powershell
python src/staging_promotion_scenario.py
```

상세 기계 판독 결과는 `results/staging_promotion_scenario/<실행번호>/scenario_result.json`에 생성됩니다.

## 해석 범위

이 시험은 데이터 변환, 격리, 후보 생성, 비교, 안전 차단이 연결되어 작동한다는 시스템 검증입니다. 실제 기업 데이터에 대한 리콜 예방률·ROI·모델 성능을 확정하는 검증은 아닙니다.
